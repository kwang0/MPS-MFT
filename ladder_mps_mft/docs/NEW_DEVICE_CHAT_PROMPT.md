# First prompt for a new Codex task or device

Copy the text below into a new Codex task opened at the `MPS-MFT` repository
root after synchronizing the intended Git revision. It is deliberately
timeless: current run state belongs in `docs/PROJECT_STATE.md`.

---

Continue the ladder MPS+MF project. Work only inside `ladder_mps_mft/` unless I
explicitly expand the scope.

Before substantive action, read these files completely and in this order:

1. `ladder_mps_mft/AGENTS.md`
2. `ladder_mps_mft/docs/README.md`
3. `ladder_mps_mft/docs/PROJECT_STATE.md`
4. `ladder_mps_mft/docs/ARCHITECTURE.md`
5. `ladder_mps_mft/docs/plans/ACTIVE.md`
6. the method and campaign documents identified there for the current task
7. only the latest relevant section of `ladder_mps_mft/docs/RUN_LOG.md`

Then inspect the current branch, commit, and working tree. Distinguish clearly
among Git-tracked code, locally synchronized compact evidence, and full
Perlmutter scratch artifacts. Treat the current user report or supplied
Perlmutter output as newer than any dated status in the repository, and state
what has and has not been verified.

Do not connect to Perlmutter, transfer data, operate Slurm, or modify either
budget ledger. Prepare and validate locally, and give me exact labeled
Perlmutter handoff commands only when requested. Do not infer submission or
cancellation authority from a plan or campaign document.

Preserve physical periodic-orbit phases separately. Rank only accepted states
with matching model, numerical, implementation, pair-binding, scalar, and
geometry provenance through the canonical variational energy including
double-counting terms. Never compare energies across transverse geometries or
different Hamiltonian points, and do not claim a thermodynamic phase from
finite-size, finite-bond-dimension Phase 1 data.

Before finishing a meaningful change, update `docs/PROJECT_STATE.md` and append
the durable evidence, validation boundary, and decision to `docs/RUN_LOG.md`.
Keep the current-state file concise rather than copying old logs into it.

---
